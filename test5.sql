create procedure add_Publisher as select * from publisher insert into publisher(pubId,pubName) 

			values (1, 'novin')

insert into publisher(pubId,pubName)
			values (2, 'zarin')

execute add_Publisher